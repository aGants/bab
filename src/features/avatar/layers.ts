import type { AccessoryId } from '@/entities/avatar/types'

/** Accessories drawn behind the head, so it covers them where they overlap. */
export const BEHIND_HEAD: readonly AccessoryId[] = ['scarf', 'hands']
